import React, { Component } from "react";
import TitleBar from "./components/TitleBar";

const Contact = () => {
    return(
        <div>
            <TitleBar/>
            <h1>Hello Contact</h1>
        </div>
    );
};

export default Contact;