import React, { Component } from 'react';
import TitleBar from './components/TitleBar.js'

const Shop = () => {
    return(
        <div>
            <TitleBar/>
            <h1>Hello Shop</h1>
        </div>
    );
};

export default Shop;